import * as React from "react";
import ReactDOM from "react-dom";
import { InjectableFormik } from "./components/InjectableFormik";

ReactDOM.render(<InjectableFormik />, document.getElementById("root"));
